n = input("Enter the number: ")
n = int(n)

numbers = []

while n != 0:
    num = n % 10
    numbers = [num] + numbers
    n = n // 10
print(numbers)
    
for num in numbers:
    n = n * 10 +num
print("The number was %d" % n)
